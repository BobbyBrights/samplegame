export default function numberLineController($scope, $state, $stateParams, GameData, RequireImages, $timeout, $gameSvg) {  'ngInject';  $scope.gamePageView = 'number-line.b';  $scope.$numberLines = $gameSvg;  var gameData = GameData.getCamelCase('levels.' + $scope.level + '.categories.' +    $stateParams.category + '.games.addition.types.number-line');  var questionData = gameData.home.question;  $scope.flag = true;  $scope.header = gameData.home.header;  $scope.questionText = questionData.hint.text;  $scope.plusSignText = questionData.sign.plusText;  $scope.equalSignText = questionData.sign.equalText;  $scope.greenCurve = questionData.greenCurve[0];  // Set image path  $scope.Images = RequireImages.get($scope.getImageContext(), gameData.home.images);  // Set menu icons  $scope.menu = _.cloneDeep(gameData.menu);  $scope.menu.left.buttons = RequireImages.get($scope.getImageContext(), gameData.menu.left.buttons);  $scope.signImages = RequireImages.get($scope.getImageContext(), questionData.signImages);  //For more/next button click  $scope.next = next;  $scope.check = check;  $scope.redo = redo;  $scope.getScoreboard = getScoreboard;  init();  var checkIsEmpty = false;  function getScoreboard(scoreboard) {    $scope.scoreboard = scoreboard;  }  //Disable special characters and alphabets  function disableCharacter() {    $timeout(function () {      $('input').on('keydown', function (event) {        var regex = new RegExp('^[]+$');        var key = String.fromCharCode(!event.charCode ? event.which : event.charCode);        if (!regex.test(key)) {          event.preventDefault();          return false;        }      });    });  }  var value = '';  $scope.onNumberClick = function ($event, num) {    if ($scope.flag === -1) {      if (num === 'backspace') {        value = value.substring(0, value.length - 1);      } else {        value = value.length <= 1 ? value + '' + num : value;      }    }    angular.element('#input-1').val(value);  };  // Game initialization function  function init() {    $scope.inputDisabled = false;    $scope.Scorecount = 0;    $scope.number = [];    $scope.checkClass = {tick: 'invisible', cross: 'invisible'};    $scope.firstNumber = $scope.generateRandomNumber(6, 0);    $scope.greenCurve = questionData.greenCurve[$scope.firstNumber - 1];    $scope.secondNumber = $scope.generateRandomNumber(6, 0);    $scope.result = eval($scope.firstNumber + $scope.secondNumber);    $timeout(function () {      $scope.number.push($scope.secondNumber);    });    $scope.flag = -1;    angular.element('.check-btns').removeClass('move-disable').addClass('move-enable');    disableCharacter();  }  // For more button click  function next($event) {    $event.preventDefault();    init();    angular.element('#input-1').val('');    value = '';    return false;  }  // For more button click  function check($event, inputVal) {    $event.preventDefault();    var answer = parseInt($('#input-1').val());    checkIsEmpty = _.isEmpty(value);    if (checkIsEmpty === false) {      $scope.inputDisabled = true;      if (answer === $scope.result) {        $scope.Scorecount++;        $scope.flag = 1;        angular.element('.check-btns').addClass('move-disable');        if ($scope.Scorecount === 1) {          $scope.scoreboard.up();        }      } else {        $scope.flag = 0;      }    }  }  function redo($event) {    $event.preventDefault();    $scope.inputDisabled = false;    angular.element('#input-1').val('');    value = '';    $scope.flag = -1;    angular.element('.check-btns').removeClass('move-disable').addClass('move-enable');    return false;  }}